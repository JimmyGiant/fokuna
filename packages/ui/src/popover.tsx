"use client";

import { Popover as PopoverPrimitive } from "radix-ui";

/** Re-export Radix Popover for feature surfaces that cannot depend on `radix-ui` directly. */
export const Popover = PopoverPrimitive;
