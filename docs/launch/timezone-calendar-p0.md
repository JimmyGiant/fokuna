# Calendar Views P0 Decision

Accepted default for the first web release:

- P0: Day (`timeGridDay`), Week (`timeGridWeek`), Month (`dayGridMonth`)
- P1: List view
- Sync providers P1 after internal calendar CRUD: Google Calendar, Microsoft 365

This matches ADR 0003 and PRD FR-CAL scope for the first production cut.
